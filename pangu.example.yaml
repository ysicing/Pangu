debug: true

db:
  type: sqlite
  dsn: "file:/tmp/pangu.db?cache=shared&mode=rwc"
  metrics:
    enable: true
    # name: "pangu"

cache:
  type: redis
  host: 127.0.0.1:6379
  password: "redis"
  # db: 6
